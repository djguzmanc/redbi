import { NullImgUrlPipe } from './null-img-url.pipe';

describe('NullImgUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new NullImgUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
